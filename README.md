# TreeStructure
"C와 C++로 구현하는 자료구조" 책에서 소개된 트리 자료구조들을 C++로 구현한 리포지토리입니다[1].

속도 등 테스트 결과는 각 단원 프로젝트의 Testing*.cpp에 작성되어있으며, 이곳 리드미에는 각 단원 트리의 간단한 개념 소개와, 구현 과정에서 얻은 여러 C++ 교훈을 적어놓았습니다. 



## 예제 실행 방법
이 리포지토리는 Visual Studio 2026를 기준으로 작성되었습니다. Visual Studio가 설치되어있지 않다면 설치해주시길 바랍니다.

1. 리포지토리를 다운로드 받습니다.

2. 로컬의 리포지토리 디렉토리에 포함된 'Tree_Cpp.sln' 솔루션 파일을 더블 클릭합니다.

3. Visual Studio가 실행되면 우측에 '솔루션 탐색기' 바를 확인합니다.
(해당 '솔루션 탐색기'바가 없을시 상단의 '보기(V)'바에서 '솔루션 탐색기(P)'를 누르면 됩니다)

4. 해당 '솔루션 탐색기' 바에서 실행해보고 싶은 단원의 트리 프로젝트를 우클릭합니다.

5. 우클릭시 나오는 메뉴에서 '시작 프로젝트로 설정'을 누르거나, 단축키 'A'를 누릅니다.

6. 예제를 실행하고자 한다면, 상단의 구성(configuration)을 "Debug" "x64"로 설정하고, 디버깅 시작키인 'F5'를 누르면 됩니다. 실행 결과가 검은 명렁 프롬프트에 출력될 것입니다. 또한 메모리 누수가 존재한다면 하단의 출력(output) 창에 누수 정보가 출력될 것입니다.

7. 속도 테스트를 실행하고자 한다면, 상단의 구성을 "Release"로 변경하고, 해당 단원 프로젝트의 Testing*.cpp 상단에 위치한 속도 테스트 상수 정의를 주석 해제한 후 디버깅 없이 실행('Ctrl+F5')을 누르면 됩니다.




## 목차  
[0. 공통적인 교훈](#0-공통적인-교훈)  
[1. 재귀 호출을 이용한 이진 탐색 트리](#1-재귀-호출을-이용한-이진-탐색-트리)  
[2. 반복문을 이용한 이진 탐색 트리](#2-반복문을-이용한-이진-탐색-트리)  
[3. 힙](#3-힙)  
[4. 스플레이 트리](#4-스플레이-트리)  
[5. AVL 트리](#5-avl-트리)   
[6. 레드-블랙 트리](#6-레드-블랙-트리)   




## 0. 공통적인 교훈

### 0.1. 표기법은 왜 통일해야 하는가?

### 0.2. 로그 출력 매크로 함수는 왜 코드의 가독성을 향상시키는가?

### 0.3. lvalue와 rvalue란 무엇인가?

### 0.4. 이동 생성자, 이동 할당 연산자가 왜 필요한가?

### 0.5. 포워딩은 왜 필요한가?

### 0.6. 예외 안전성과 noexcept은 왜 필요한가?

### 0.7. 자료구조의 속도는 어떻게 측정하는가? (feat. 호출트리)

### 0.8. Visual Studio에서 어셈블리는 어떻게 분석하는가?

### 0.9. Visual Studio에서 메모리 누수는 어떻게 탐지하는가?




## 1. 재귀 호출을 이용한 이진 탐색 트리
이진 탐색 트리(Binary Search Tree, BST)는 각 노드의 키 값을 기준으로 자식 노드들의 대소 관계가 지켜지는 트리입니다. 부모노드의 키(key)는 좌측 자식 노드의 키보다는 크고, 우측 자식의 키보다는 작아야 한다는 조건을 통해 빠르게 특정 키의 노드를 찾을 수 있습니다. 평균적으로 O(log(n))의 시간 복잡도로 빠르게 데이터의 삽입-검색-삭제를 할 수 있습니다만, 키가 이미 정렬된 순서로 노드들이 삽입된다면 트리가 연결 리스트처럼 일렬로 자라나므로 O(n)의 시간 복잡도를 가지게 됩니다.

이진 탐색 트리는 부모와 자식 간의 관계가 재귀적으로 반복되므로, 재귀를 통해 편하게 구현할 수 있습니다. 함수 호출 기능이 호출 함수와 피호출 함수의 변수 스코프를 격리해주기 때문에, 이러한 함수 호출 기능을 이용하는 재귀를 사용하면 큰 자료구조를 신경쓰지 않고 현재 노드와 자식 노드 등 작은 영역만을 고려하여 쉽게 구현할 수 있습니다. 다만 함수 스택은 프로세스에 주어진 가상 메모리 중 그 크기가 힙 영역에 비해 작은 스택 영역에만 쌓이기에, 재귀 호출이 깊어지면 더 이상 함수 스택을 저장할 스택 영역이 부족하여 스택 오버플로우가 발생할 수 있습니다.

### 1.1. 퍼블릭 메소드와 재귀 구현 메소드는 왜 구분해야 하는가?




## 2. 반복문을 이용한 이진 탐색 트리
재귀는 프로그래머가 직관적으로 프로그래밍을 할 수 있도록 하지만, 재귀를 위한 반복적인 함수 호출에는 비용이 따릅니다. 호출 함수의 상태를 보존하기 위해 이들을 가상 메모리의 스택 영역에 저장하고, 또 함수 호출이 끝나면 이들을 레지스터로 다시 돌려놓는데 시간이 쓰이기 때문입니다. 수많은 데이터를 삽입-검색-삭제를 반복하는 자료구조에서는 이러한 재귀 호출로 인한 비용을 최적화하는 것이 좋습니다.

함수 호출 기능에 의존하는 재귀호출 대신, 직접 스택과 반복문을 사용해 더 가벼운(다만 사람이 읽고 쓰기엔 더 복잡한) 코드를 작성할 수 있습니다. 이 단원에서는 이처럼 이진 탐색 트리를 재귀가 아닌 반복문으로 구현하고, 또 반복적으로 사용되는 탐색과 순회 로직을 제너릭 메소드로 일반화하는 과정, 그리고 이진 탐색 트리를 상속하여 다른 단원의 트리를 구현할 수 있도록 트리의 템플릿을 일반화하는 과정을 다룹니다.

### 2.1. 재귀로 구현된 로직을 어떻게 반복문으로 재작성하는가?

### 2.2. 어떻게 예외를 발생시키지 않는 소멸자를 반복문으로 작성하는가?

### 2.3. 제너릭 메소드는 어떻게 작성하는가?

### 2.4. 트리 자료구조는 어떻게 상속해야 하는가?




## 3. 힙
힙(Heap)은 부모 노드와 자식 노드 사이에 대소 관계가 지켜지는 완전 이진 트리입니다. 부모가 자식보다 커야 한다면 MaxHeap이라 하고, 작아야 한다면 MinHeap이라고 합니다. MaxHeap에서 루트 노드는 모든 노드 중 가장 큰 키를 가진 노드이므로, 가장 키가 큰 요소를 반환하는 우선순위 큐로 활용할 수 있습니다. MinHeap은 마찬가지로, 가장 키가 작은 요소를 반환하는 우선순위 큐로 활용할 수 있습니다.

힙은 최대한 자식들을 채워넣는 완전 이진 트리이므로 어레이 구조를 사용하여도 메모리 공간의 낭비가 크지 않으며, 간단한 인덱스 연산으로 부모와 자식 간 이동이 가능하다는 장점이 있습니다.




## 4. 스플레이 트리
특정 노드를 검색할 때마다 해당 노드를 루트에 가까운 위치로 올려, 자주 검색되는 노드가 더 빨리 검색되도록 하는 이진탐색트리의 일종입니다.




## 5. AVL 트리
이진 탐색 트리에서 키가 정렬된 노드들이 삽입될 때 발생하는 편향 문제를 해결한 균형 트리구조입니다. 노드의 왼쪽 하위 트리의 높이와 우측 하위 트리의 높이의 차가 2 이상이 될 경우에, 해당 위치를 기준으로 노드들의 회전을 적용해 트리의 균형을 조절합니다.




## 6. 레드-블랙 트리
완벽하게 균형이 지켜지는 2-3-4 트리의 개념을, 이진 탐색 트리에서 모방 구현한 균형 추종 트리입니다.  

2-3-4 트리는 기존에 한 개의 레코드가 들어가는 노드(2-노드) 외에도, 한 노드 안에 레코드가 2개 들어가는 노드(3-노드)와, 3개 들어가는 노드(4-노드)들을 함께 사용하여 삽입과 삭제에도 완벽하게 균형이 유지되도록 한 트리입니다.

레드-블랙 트리는 3-노드나 4-노드를 사용하지 않고, 2-노드에 '레드'와 '블랙'이라는 색깔 멤버 변수를 추가하여, 3-노드를 인접한 두 개의 2-노드로 구현하고, 4-노드를 인접한 3개의 2-노드로 구현합니다. 2-3-4 트리의 개념에서는 각각 하나의 노드인 3-노드와 4-노드가, 레드-블랙 트리에서는 높이를 가진 작은 트리와 같이 구현되기에, 레드-블랙 트리는 균형을 완벽하게 지키지는 못합니다. 그러나 2-3-4 트리의 개념을 추종하기에 균형을 어느 정도 지키고 있으며, 노드의 클래스 타입이 다를 수 있는 2-3-4 트리 개념을 그대로 구현하는 것보다는 이진 탐색 트리의 로직을 재활용해 비교적 쉽게 구현이 가능하다는 장점을 가집니다.




## 참고문헌
[1] "C와 C++로 구현하는 자료구조" (주우석 저 / 한빛아카데미 출판)